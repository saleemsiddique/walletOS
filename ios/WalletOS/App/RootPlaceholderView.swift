import SwiftUI

/// Placeholder raíz del scaffold (Rama 1). Se sustituye por el AppRouter/RootTabView en la Rama 15.
struct RootPlaceholderView: View {
    var body: some View {
        Text("WalletOS")
            .font(.largeTitle.weight(.semibold))
    }
}

#Preview {
    RootPlaceholderView()
}
