meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}
