meta {
  name: Create Bank
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/banks
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "Santander"
  }
}
