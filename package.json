{
  "name": "walletos",
  "version": "0.0.1",
  "private": true,
  "description": "WalletOS monorepo root — tooling and git hooks only",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.0.0",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.3"
  }
}
